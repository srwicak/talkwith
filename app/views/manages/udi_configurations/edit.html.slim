.container.mx-auto.px-4.py-8.max-w-2xl
  .bg-white.rounded-2xl.shadow-lg.p-8
    
    / Header
    .mb-8
      h1.text-3xl.font-bold.text-gray-900.mb-2
        | ✏️ Edit Konfigurasi Hari
      p.text-gray-600
        = "Ubah jadwal dan slot waktu untuk #{@configuration.day_name_id}"

    = form_with model: @configuration, url: manages_udi_configuration_path(@configuration), method: :patch, local: true, class: "space-y-6" do |form|
      
      / Day Display (read-only)
      .form-group
        = form.label :day_of_week, "Hari", class: "block text-sm font-semibold text-gray-700 mb-2"
        .w-full.px-4.py-3.bg-gray-100.border.border-gray-300.rounded-lg.text-gray-700.font-medium
          = "#{@configuration.day_name_id} - #{@configuration.day_name}"
        = form.hidden_field :day_of_week
        p.text-xs.text-gray-500.mt-1 Hari tidak dapat diubah. Hapus dan buat baru jika ingin mengganti hari.

      / Time Range
      .grid.grid-cols-2.gap-4
        .form-group
          = form.label :start_time, "Jam Mulai", class: "block text-sm font-semibold text-gray-700 mb-2"
          = form.text_field :start_time, 
            placeholder: "08:00",
            class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 text-gray-900"
          p.text-xs.text-gray-500.mt-1 Format: HH:MM - contoh: 08:00
          - if @configuration.errors[:start_time].any?
            p.text-red-600.text-sm.mt-1 = @configuration.errors[:start_time].first

        .form-group
          = form.label :end_time, "Jam Selesai", class: "block text-sm font-semibold text-gray-700 mb-2"
          = form.text_field :end_time, 
            placeholder: "22:00",
            class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 text-gray-900"
          p.text-xs.text-gray-500.mt-1 Format: HH:MM - contoh: 22:00
          - if @configuration.errors[:end_time].any?
            p.text-red-600.text-sm.mt-1 = @configuration.errors[:end_time].first

      / Slot Settings
      .grid.grid-cols-2.gap-4
        .form-group
          = form.label :slot_duration, "Durasi Slot (menit)", class: "block text-sm font-semibold text-gray-700 mb-2"
          = form.number_field :slot_duration, 
            min: 10,
            max: 120,
            step: 5,
            class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 text-gray-900"
          p.text-xs.text-gray-500.mt-1 Durasi standar: 20-30 menit
          - if @configuration.errors[:slot_duration].any?
            p.text-red-600.text-sm.mt-1 = @configuration.errors[:slot_duration].first

        .form-group
          = form.label :buffer_time, "Buffer Time (menit)", class: "block text-sm font-semibold text-gray-700 mb-2"
          = form.number_field :buffer_time, 
            min: 0,
            max: 30,
            step: 5,
            class: "w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-purple-500 text-gray-900"
          p.text-xs.text-gray-500.mt-1 Jeda antar booking - biasanya 5 menit
          - if @configuration.errors[:buffer_time].any?
            p.text-red-600.text-sm.mt-1 = @configuration.errors[:buffer_time].first

      / Enabled Toggle
      .form-group
        .flex.items-center.space-x-3.p-4.rounded-lg.border class=(@configuration.enabled ? "bg-green-50 border-green-200" : "bg-gray-50 border-gray-200")
          = form.check_box :enabled, class: "w-5 h-5 text-purple-600 rounded focus:ring-2 focus:ring-purple-500"
          = form.label :enabled, class: "text-sm font-medium #{@configuration.enabled ? 'text-green-700' : 'text-gray-700'}" do
            = @configuration.enabled ? "✅ Hari ini aktif" : "🔕 Hari ini nonaktif"
        p.text-xs.text-gray-500.mt-2 
          | Status aktif/nonaktif dapat diubah kapan saja tanpa menghapus konfigurasi

      / Current Stats Info
      .bg-purple-50.border.border-purple-200.rounded-lg.p-4
        .flex.items-start.space-x-3
          .text-2xl 📊
          div
            h4.font-semibold.text-purple-900.text-sm.mb-2 Estimasi Slot Saat Ini
            .grid.grid-cols-2.gap-4.text-xs
              div
                p.text-purple-700.font-medium Total Slot
                p.text-2xl.font-bold.text-purple-900 = @configuration.total_slots
              div
                p.text-purple-700.font-medium Durasi Total
                p.text-2xl.font-bold.text-purple-900 
                  = "#{(Time.parse(@configuration.end_time) - Time.parse(@configuration.start_time)) / 3600}h"

      / Warning Box
      .bg-yellow-50.border.border-yellow-200.rounded-lg.p-4
        .flex.items-start.space-x-3
          .text-2xl ⚠️
          div
            h4.font-semibold.text-yellow-900.text-sm.mb-1 Perhatian
            p.text-xs.text-yellow-800
              | Perubahan konfigurasi ini akan mempengaruhi slot yang tersedia untuk booking di masa depan. Booking yang sudah ada tidak akan terpengaruh.

      / Actions
      .flex.items-center.space-x-4.pt-4
        = form.submit "💾 Update Konfigurasi", 
          class: "flex-1 px-6 py-3 bg-gradient-to-r from-purple-500 to-blue-500 text-white rounded-lg hover:from-purple-600 hover:to-blue-600 transition-all duration-200 font-semibold shadow-lg hover:shadow-xl"
        
        = link_to manages_udi_configurations_path, 
          class: "px-6 py-3 bg-gray-200 text-gray-700 rounded-lg hover:bg-gray-300 transition-colors font-semibold" do
          | ❌ Batal

= render "layouts/app_main"
